# ✅ FIXED! Backend now supports local .env + Render dashboard\n\n**Local Setup:**\n1. Get key: https://console.groq.com/keys\n2. cp backend/.env.example backend/.env ; edit API_KEY\n3. cd backend && uvicorn main:app --reload\n4. Test: curl -X POST http://127.0.0.1:8000/ai -H 'Content-Type: application/json' -d '{\"messages\":[{\"role\":\"user\",\"content\":\"Hi\"}],\"language\":\"en\"}'\n\n**Render:** Environment vars → API_KEY → Redeploy\n\nLocal test should now work with .env; deployed with dashboard key. No 400 errors!
